var bodyDiv = document.getElementById('body');

/*
for (var i = 0; i < 1; i++) {
	var tabTmpl = document.getElementById('tab-template').content.cloneNode(true);
	bodyDiv.appendChild(tabTmpl);
}
*/

for (var i = 0; i < 1; i++) {
	var photoTmpl = document.getElementById('photo-template').content.cloneNode(true);
	bodyDiv.appendChild(photoTmpl);
}